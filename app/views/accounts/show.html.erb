<div class='decks'>
  <h2>Decks you've created</h2>
  <p class='note'>
    <%= link_to('Create', new_deck_path) %> a new deck.
  </p>
  <ul>
    <% @account.decks.each do |deck| %>
      <%= render :partial => '/decks/summary', :locals => {:deck => deck} %>
    <% end %>
  </ul>
  
  <br>
  
  <h2>Public decks you've subscribed to</h2>
  <% if @account.subscriptions.size > 0  %>
    <p class='note'>
      <%= link_to('Browse', decks_path) %> for additional public decks.
    </p>
  <% end %>

  <ul>
    <% @account.subscriptions.each do |deck| %>
      <%= render :partial => '/decks/summary', :locals => {:deck => deck} %>
    <% end %>
  </ul>
</div>
